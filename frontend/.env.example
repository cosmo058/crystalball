# Local development only — points the browser directly at the backend.
# Leave empty (or delete this file) when running via Docker Compose;
# nginx handles the proxy automatically.
VITE_API_URL=http://localhost:8000
